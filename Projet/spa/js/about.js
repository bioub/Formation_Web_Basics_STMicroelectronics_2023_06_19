function about(mainEl) {
    const template = `
<p>ST Project Web Basics v1.0</p>    
`
mainEl.innerHTML = template;

// const pEl = document.createElement('p');
// pEl.innerText = 'ST Project Web Basics v1.0';
// mainEl.append(pEl);
}